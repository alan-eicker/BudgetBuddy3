mutation addExpense(
  $name: String!
  $balance: Float!
  $dueDate: String!
  $isPaid: Boolean!
) {
  addExpense(
    name: $name
    balance: $balance
    dueDate: $dueDate
    isPaid: $isPaid
  ) {
    _id
    name
    balance
    dueDate
    isPaid
  }
}
